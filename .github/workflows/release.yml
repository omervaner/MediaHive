name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    name: Build ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: macos-latest
            artifact-name: macos
          - os: windows-latest
            artifact-name: windows

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build application
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: "false"
        run: npm run electron:dist

      - name: Prepare artifacts
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p release-assets

          if [ "${{ matrix.os }}" = "macos-latest" ]; then
            find dist -maxdepth 1 -type f -name "MediaHive-*-mac.dmg" -exec cp {} release-assets/ \;
            find dist -maxdepth 1 -type f -name "MediaHive-*-mac.zip" -exec cp {} release-assets/ \;
          else
            find dist -maxdepth 1 -type f -name "MediaHive-*-win-x64.exe" -exec cp {} release-assets/ \;
            find dist -maxdepth 1 -type f -name "MediaHive-*-win-x64-Portable.exe" -exec cp {} release-assets/ \;
          fi

      - name: List artifacts
        shell: bash
        run: |
          echo "Release assets:"
          ls -la release-assets/ || echo "No release-assets directory found"

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: mediahive-${{ matrix.artifact-name }}
          path: release-assets/
          if-no-files-found: error

  release:
    name: Create Release
    runs-on: ubuntu-latest
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: all-artifacts

      - name: Prepare release assets
        run: |
          set -euo pipefail

          mkdir -p release-files

          find all-artifacts -maxdepth 3 -type f -name "MediaHive-*-mac.dmg" -exec cp {} release-files/ \;
          find all-artifacts -maxdepth 3 -type f -name "MediaHive-*-mac.zip" -exec cp {} release-files/ \;
          find all-artifacts -maxdepth 3 -type f -name "MediaHive-*-win-x64.exe" -exec cp {} release-files/ \;
          find all-artifacts -maxdepth 3 -type f -name "MediaHive-*-win-x64-Portable.exe" -exec cp {} release-files/ \;

          echo "Final release files:"
          ls -la release-files/

          FILE_COUNT=$(find release-files -type f | wc -l)
          if [ "$FILE_COUNT" -ne 4 ]; then
            echo "Expected 4 release files, found $FILE_COUNT"
            exit 1
          fi

      - name: Generate release notes with checksums
        id: release_notes
        run: |
          set -euo pipefail

          TAG_NAME="${{ github.ref_name }}"

          pushd release-files >/dev/null
          sha256sum * | sort > ../checksums.txt
          popd >/dev/null

          cat > release_notes.md << EOF
          # MediaHive $TAG_NAME

          ## Installation
          - **macOS**
            - \`MediaHive-*-mac.dmg\`: Drag-and-drop installer (recommended)
            - \`MediaHive-*-mac.zip\`: Zipped .app bundle
          - **Windows**
            - \`MediaHive-*-win-x64.exe\`: Installer (recommended)
            - \`MediaHive-*-win-x64-Portable.exe\`: Portable build (no installation)

          ## Checksums (SHA256)

          \`\`\`
          $(cat checksums.txt)
          \`\`\`
          EOF

      - name: Create Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG_NAME="${{ github.ref_name }}"

          PRERELEASE_FLAG=""
          if [[ "$TAG_NAME" == *"-rc"* ]] || [[ "$TAG_NAME" == *"-beta"* ]] || [[ "$TAG_NAME" == *"-alpha"* ]]; then
            PRERELEASE_FLAG="--prerelease"
          fi

          gh release create "$TAG_NAME" \
            --draft \
            $PRERELEASE_FLAG \
            --title "MediaHive $TAG_NAME" \
            --notes-file release_notes.md \
            release-files/*
